# E2EE-Java-Chat-Protocol
A multi-threaded Java chat application featuring a custom End-to-End Encryption (E2EE) protocol built from scratch. It bypasses standard secure sockets to implement a hybrid cryptographic architecture (2048-bit RSA for handshakes, 256-bit AES for messages) over raw TCP sockets, ensuring the server acts as a zero-knowledge relay.
